package org.praxisplatform.uischema.action;

/**
 * Interaction that a runtime should materialize before executing a workflow action.
 */
public enum ActionInteractionMode {
    DIRECT,
    CONFIRM,
    FORM
}
