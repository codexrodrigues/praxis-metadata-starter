/**
 * Referencias canonicas de schema estrutural resolvidas por {@code /schemas/filtered}.
 *
 * <p>
 * Este pacote concentra a identidade estrutural do schema publicado pelo starter, preservando a
 * coerencia entre {@code schemaId}, {@code schemaUrl} e tipo logico de schema.
 * </p>
 *
 * <p>
 * A regra central e que qualquer dimensao estrutural nova precisa aparecer tanto na URL quanto no
 * identificador canonico, evitando drift entre discovery e payload estrutural.
 * </p>
 */
package org.praxisplatform.uischema.schema;
