package org.praxisplatform.uischema.stats;

/** Strategy used to derive the period preceding the current comparison window. */
public enum ComparisonPeriodMode {
    PREVIOUS_ALIGNED,
    PREVIOUS_CALENDAR_PERIOD
}
