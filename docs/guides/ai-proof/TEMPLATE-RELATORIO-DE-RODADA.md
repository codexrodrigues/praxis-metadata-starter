# Relatorio de Rodada de Prova

## Identificacao

- rodada:
- data:
- executor:
- llm:
- versao do guia:
- projeto sandbox:
- banco usado:

## Material entregue a LLM

- guias liberados:
- prompt usado:
- restricoes adicionais:

## Resultado

- status geral: aprovado | reprovado
- build: aprovado | reprovado
- startup: aprovado | reprovado
- contrato de schemas: aprovado | reprovado
- CRUD baseline: aprovado | reprovado
- options: nao aplicavel | aprovado | reprovado
- consumo Angular: nao executado | aprovado | reprovado

## Evidencias minimas

- comando de build:
- comando de startup:
- endpoint `GET /v3/api-docs`:
- endpoint `GET /{resource}/schemas`:
- endpoint `GET /schemas/filtered?...response`:
- endpoint `GET /schemas/filtered?...request`:
- endpoint `POST /{resource}`:
- endpoint `POST /{resource}/filter`:
- endpoint `POST /{resource}/options/filter`:
- endpoint `GET /{resource}/options/by-ids`:

## Falhas encontradas

### Falha 1

- categoria:
- sintoma:
- evidencia:
- causa provavel:
- impede aprovacao: sim | nao
- ajuste proposto:

### Falha 2

- categoria:
- sintoma:
- evidencia:
- causa provavel:
- impede aprovacao: sim | nao
- ajuste proposto:

## Divergencias entre guia e codigo

- 

## Divergencias entre guia e comportamento da LLM

- 

## Acoes apos a rodada

- ajustes no guia:
- ajustes no checklist:
- ajustes no prompt:
- ajuste necessario no starter:

## Veredito

- pode repetir a mesma rodada sem mudar prompt: sim | nao
- pronto para proxima rodada: sim | nao
- ponto exato de retomada:
